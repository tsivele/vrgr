# Αρχιτεκτονική

## Γιατί έτσι

Το πρόβλημα δεν είναι «να γραφτεί μια καλή λεζάντα». Είναι **να αποδειχθεί
ότι μια λεζάντα είναι καλύτερη από μια άλλη**, για συγκεκριμένο κοινό, με
δεδομένα και όχι με γνώμη.

Κάθε δομική απόφαση εξυπηρετεί αυτό.

---

## Ροή δεδομένων

```
                        αρχείο βίντεο
                              │
        ┌─────────────────────▼─────────────────────┐
        │  video/  — ΝΤΕΤΕΡΜΙΝΙΣΤΙΚΑ, χωρίς LLM      │
        │  probe → σκηνές → καρέ (πυκνά στο hook)   │
        │  audio → ASR (προαιρετικό)                │
        └─────────────────────┬─────────────────────┘
                              │ VideoTechnical  [OBSERVED]
        ┌─────────────────────▼─────────────────────┐
        │  video/analyzer  — LLM με forced schema   │
        │  περιεχόμενο + σήματα + 3-5 ΓΩΝΙΕΣ        │
        └─────────────────────┬─────────────────────┘
                              │ VideoAnalysis   [INFERRED]
        ┌─────────────────────▼─────────────────────┐
        │  research/planner  — ελληνικοί όροι       │
        │  σταθερή βάση (config) + δυναμική (LLM)   │
        └─────────────────────┬─────────────────────┘
                              │ ResearchQuery[]
        ┌─────────────────────▼─────────────────────┐
        │  research/collector  →  HikerAPI          │
        │  hashtags · keywords · creators           │
        │  + ΕΜΠΛΟΥΤΙΣΜΟΣ followers                 │
        │  + κανονικοποίηση ως προς μέγεθος         │
        └──────────┬───────────────────┬────────────┘
                   │ [OBSERVED]        │
        ┌──────────▼──────────┐   ┌────▼──────────────┐
        │  memory/  SQLite    │   │ research/mining   │
        │  posts + snapshots  │◄──┤ ανάλυση ΑΝΥΨΩΣΗΣ  │
        │  patterns Beta(α,β) │   │ outliers vs βάση  │
        │  FTS5 + embeddings  │   └────┬──────────────┘
        └──────────┬──────────┘        │ MinedPatterns [DERIVED]
                   │ υβριδική ανάκτηση │
        ┌──────────▼───────────────────▼────────────┐
        │  generation/                              │
        │  captions  → 6-10 υποψήφιες + έλεγχοι     │
        │  hashtags  → 4 ΧΑΡΤΟΦΥΛΑΚΙΑ σε επίπεδα    │
        └─────────────────────┬─────────────────────┘
                              │ N × M ζεύγη
        ┌─────────────────────▼─────────────────────┐
        │  scoring/  7 πυλώνες → evidence ×         │
        │  ranking/  ΚΟΙΝΗ βελτιστοποίηση + συνέργεια│
        └─────────────────────┬─────────────────────┘
                              │
                    ΕΝΑΣ νικητής + εφεδρικά
                              │
                       report/ (ελληνικά)
                              │
                     learning/feedback ──► πίσω στη μνήμη
```

---

## Δομή φακέλων

```
src/vrgr/
├── config.py            ρυθμίσεις — μόνο από environment/.env
├── logging_setup.py     logging με ΥΠΟΧΡΕΩΤΙΚΟ redaction μυστικών
├── errors.py            ιεραρχία σφαλμάτων (κάθε ένα ξέρει αν αξίζει retry)
├── schemas.py           κανονικά μοντέλα — κάθε αριθμός με Origin
├── greek.py             ελληνική γλώσσα (τόνοι, τελικό σίγμα, greeklish)
│
├── clients/
│   ├── hiker/
│   │   ├── endpoints.py  ΕΠΑΛΗΘΕΥΜΕΝΟ μητρώο 27 endpoints + ΜΗ διαθέσιμα
│   │   ├── client.py     retries · rate limit · budget · cache · fallback
│   │   └── normalize.py  αναδρομικός walker για 5 οικογένειες σχημάτων
│   └── llm/
│       ├── base.py       output_config.format → εγγυημένα έγκυρο JSON
│       └── embeddings.py local (default) | voyage | openai
│
├── cache/store.py       επίμονο TTL cache (credits = χρήμα)
├── video/               ffmpeg · probe · frames · audio · analyzer
├── analysis/metrics.py  V/F, viral multiplier, outlier score
├── research/            planner · collector · mining
├── memory/              db · repository · retrieval · patterns
├── generation/          captions · hashtags · prompts
├── scoring/             viral_score · ranking
├── learning/            feedback · calibration
├── pipeline/            orchestrator (τα 12 βήματα, με callback προόδου)
├── web/
│   ├── server.py        HTTP server — ΜΟΝΟ stdlib, δέσμευση σε 127.0.0.1
│   ├── jobs.py          εργασίες παρασκηνίου με ορατή πρόοδο
│   └── static/          index.html + app.js (ελληνική διεπαφή)
├── maintenance.py       καθαρισμός δίσκου (ποτέ τη βάση γνώσης)
├── report.py            ελληνική παρουσίαση με σήμανση προέλευσης
└── cli.py               γραμμή εντολών

config/
├── weights.json         βάρη σκορ — ΔΕΝ είναι hardcoded
├── niches.json          niche → ελληνικοί όροι έρευνας
└── seed_creators.json   benchmark λογαριασμοί
```

---

## Οι εννέα αποφάσεις που ορίζουν το σύστημα

### 1. Κάθε αριθμός κουβαλά την προέλευσή του

`Origin` = `OBSERVED` | `DERIVED` | `INFERRED`.

Δεν είναι διακοσμητικό: το report αρνείται να παρουσιάσει `INFERRED` τιμή ως
γεγονός, και ο **evidence multiplier** τιμωρεί αποφάσεις με λίγα `OBSERVED`.
Χωρίς αυτό, μια εκτίμηση μοντέλου διαβάζεται σαν μέτρηση — που είναι ακριβώς
η κατασκευή δεδομένων που απαγορεύεται.

### 2. Το μητρώο endpoints είναι φράγμα, όχι τεκμηρίωση

Ο client δέχεται **μόνο** `Endpoint` αντικείμενα από το `endpoints.py`, όλα
διασταυρωμένα με το επίσημο `openapi.json` (154 paths). Άγνωστο path → εξαίρεση.
Άγνωστη παράμετρος → εξαίρεση. Είναι **δομικά αδύνατο** να εφευρεθεί endpoint.

Το `UNAVAILABLE` dict τεκμηριώνει ρητά τι **δεν** υπάρχει, ώστε να μη «θυμηθεί»
κανείς — άνθρωπος ή μοντέλο — ανύπαρκτη δυνατότητα.

`./run.sh verify-endpoints` επαναλαμβάνει τη διασταύρωση με το ζωντανό spec.

### 2β. Η πληρότητα των endpoints είναι ΜΕΤΡΗΜΕΝΗ, όχι υποτιθέμενη

Το `openapi.json` λέει ποια endpoints υπάρχουν, όχι **τι πραγματικά
επιστρέφουν**. Μετρήθηκε σε ζωντανό API (30/08/2026, λογαριασμός
`@kate_pavli`, hashtag `#ελληνικοχιουμορ`):

| endpoint | λεζάντα | likes | σχόλια | views | followers | ημ/νία |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| `/v2/user/clips` | ✓ | ✓ | ✓ | ✓ | ✗ | ✓ |
| `/v1/user/clips/chunk` | ✓ | ✓ | ✓ | ✓ | ✗ | ✗ |
| `/gql/user/clips?sort_by_views` | **✗** | **✗** | **✗** | ✓ | ✗ | **✗** |
| `/v2/hashtag/medias/top` | ✓ | ✓ | ✓ | 79% | ✗ | ✓ |
| `/v1/hashtag/medias/clips/chunk` | ✓ | ✓ | ✓ | ✓ | ✗ | ✗ |
| `/v2/fbsearch/reels` | 67% | ✓ | ✓ | ✓ | ✗ | ✓ |

Τρία συμπεράσματα που άλλαξαν τον κώδικα:

1. **Το `/gql/user/clips` είναι ευρετήριο, όχι πηγή.** Επιστρέφει περικομμένο
   αντικείμενο — μόνο `pk`, `play_count`, `video_duration`. Είναι ο μόνος
   τρόπος να δεις τα Reels ταξινομημένα κατά προβολές, αλλά τα δεδομένα
   πρέπει να έρθουν μετά από `/v2/media/info/by/id`. Αν το χρησιμοποιούσαμε
   ως κύρια πηγή —όπως έδειχνε λογικό από το spec— κάθε λεζάντα θα ήταν κενή
   και όλη η εξόρυξη μοτίβων θα δούλευε στο κενό, **χωρίς κανένα σφάλμα**.
2. **ΚΑΝΕΝΑ endpoint δεν δίνει `follower_count`** στο ενσωματωμένο `user`
   (0/N σε όλα). Το στάδιο εμπλουτισμού δεν είναι βελτιστοποίηση — χωρίς
   αυτό δεν υπάρχει V/F, δηλαδή δεν υπάρχει σύστημα.
3. **Τα `/v1/*/chunk` δεν έχουν `taken_at`.** Όπου μετράει η φρεσκάδα
   (σήμα τάσης, ηλικία τεκμηρίων) προηγούνται τα `/v2`.

### 3. Αναδρομικός walker αντί για parsers ανά σχήμα

154 endpoints σε 5 οικογένειες (`v1`, `v2`, `gql`, `g1/g2`, `a2`) επιστρέφουν
τα ίδια δεδομένα με ασύμβατες δομές. Αντί για parser ανά σχήμα, ο
`normalize.py` αναγνωρίζει ένα media/user dict από την **υπογραφή των πεδίων** του.

Νέο σχήμα δουλεύει χωρίς αλλαγή κώδικα. *(Ένα test βρήκε ότι έλειπαν τα πεδία
GraphQL από τις υπογραφές — που σήμαινε ότι το `/gql/user/clips`, το πιο
κρίσιμο endpoint για εντοπισμό viral, επέστρεφε σιωπηλά μηδέν posts.)*

### 4. Στιγμιότυπα, όχι εγγραφές

Το HikerAPI δίνει **στιγμιότυπο**, όχι ιστορικό. Ένα `media_id` = μία γραμμή
στο `posts` (στατικά) + Ν γραμμές στο `post_snapshots` (μετρήσεις σε χρόνο).

Έτσι το ιστορικό **χτίζεται δωρεάν**: ταχύτητα ανάπτυξης views, καμπύλες,
ανίχνευση τι μεγάλωσε γρήγορα — δεδομένα που κανένα endpoint δεν δίνει.

Φίλτρο θορύβου: νέο snapshot μόνο αν πέρασαν 6 ώρες ή άλλαξαν τα views ≥2%.

### 5. Beta(α, β) αντί για ποσοστά

Ένα μοτίβο «2/2 = 100%» και ένα «41/60 = 68%» δεν είναι το ίδιο πράγμα.

Κάθε μοτίβο κρατά `alpha` (επιτυχίες) και `beta` (αποτυχίες). Το σκορ
χρησιμοποιεί το **κάτω φράγμα** (~5ο εκατοστημόριο), όχι τη μέση τιμή — ένα
μοτίβο πρέπει να **κερδίσει** την επιρροή του. Κάτω από 4 δείγματα δεν
επηρεάζει καθόλου.

**Χρονική απόσβεση** half-life 90 ημερών, εφαρμοσμένη κατά την **ανάγνωση** —
οπότε δεν χρειάζεται εργασία συντήρησης και είναι πάντα σωστή.

### 6. Υβριδική ανάκτηση με RRF

Σημασιολογική αναζήτηση χάνει ακριβείς όρους· λεξιλογική χάνει παραφράσεις.
Στα ελληνικά το πρόβλημα οξύνεται (πλούσια μορφολογία, μικρά κείμενα).

Σύντηξη με **Reciprocal Rank Fusion** — δουλεύει με θέσεις αντί για σκορ, οπότε
δεν χρειάζεται κοινή κλίμακα για δύο ασύμβατα μέτρα.

Ο **local embedder** (hashing vectorizer πάνω σε χαρακτηρο-n-grams) είναι
default επειδή: η Anthropic δεν έχει embeddings API, και για ελληνικές
λεζάντες 10-40 λέξεων τα n-grams απορροφούν την κλίση («καφές/καφέδες/καφέ»)
— κάτι που τα word embeddings συχνά χάνουν σε μορφολογικά πλούσιες γλώσσες.

### 7. Ανάλυση ανύψωσης, όχι συχνότητας

Η λέξη «μου» εμφανίζεται στο 60% των επιτυχημένων λεζαντών — και στο 60% των
αποτυχημένων. Δεν εξηγεί τίποτα.

```
lift = P(χαρακτηριστικό | outlier) / P(χαρακτηριστικό | βάση)
```

Με εξομάλυνση Laplace και ελάχιστο πλήθος εμφανίσεων — αλλιώς ένα
χαρακτηριστικό με 2 εμφανίσεις στα outliers και 0 στη βάση θα έβγαζε άπειρο
lift και θα κυριαρχούσε λανθασμένα.

### 8. Χαρτοφυλάκιο hashtags, όχι λίστα

Τα hashtags δεν είναι ανεξάρτητες επιλογές — είναι **κατανομή ρίσκου**:

| Επίπεδο | Παράδειγμα | Ρόλος |
|---|---|---|
| broad (>1M) | `#ελλαδα` | λοταρία — τεράστιο ταβάνι, μηδενική πιθανότητα |
| mid (100K-1M) | `#greekmemes` | ρεαλιστικός στόχος |
| niche (10K-100K) | `#ελληνικοχιουμορ` | **εδώ κερδίζεις** |
| micro (<10K) | `#ζευγαρακια` | σχεδόν σίγουρη κατάταξη |

Μόνο πλατιά = μηδέν εμφανίσεις. Μόνο στενά = εμφανίσεις σε 200 άτομα.
Η αξία είναι στη **σύνθεση**. Τέσσερις στρατηγικές, με τεκμήρια βαρύτερα (35%)
από τη δημοτικότητα.

### 8β. Οι παράμετροι μοντέλου επιλέγονται από δυνατότητες, όχι στατικά

Το `budget_tokens` απορρίπτεται με **400** στα Opus 5 / 4.8 / 4.7, Sonnet 5 και
Fable 5· το ίδιο και τα `temperature` / `top_p` / `top_k`. Στατικές παράμετροι
σημαίνουν ότι μια αλλαγή μοντέλου στο `.env` σπάει ολόκληρο το σύστημα.

Γι' αυτό το `clients/llm/base.py` ρωτά τι υποστηρίζει το μοντέλο:
adaptive thinking + `output_config.effort` για τα σύγχρονα, `temperature` για
τα παλαιότερα. Η δομημένη έξοδος γίνεται με `output_config.format`
(JSON Schema), και το `strictify()` μετατρέπει τα σχήματα ώστε να περνούν το
strict mode — μεταφέροντας τις μη εγγυημένα υποστηριζόμενες λέξεις-κλειδιά
(`minItems`, `maximum`, …) στο `description` αντί να ρισκάρει 400.

### 9. Κοινή βελτιστοποίηση λεζάντας × hashtags

Η καλύτερη λεζάντα + το καλύτερο σετ ≠ ο καλύτερος συνδυασμός.

Λεζάντα **περιέργειας** χρειάζεται στενά hashtags — το κοινό πρέπει να έχει
ήδη ενδιαφέρον, αλλιώς η υπόσχεση πέφτει στο κενό και το retention καταρρέει.
Λεζάντα **ταύτισης** δουλεύει με πλατύτερα — η ταύτιση λειτουργεί σε άγνωστο
κοινό.

Γι' αυτό σκοράρονται **ζεύγη**, με ρητό όρο συνέργειας.

### 10. Τα hashtags φιλτράρονται πριν σκοραριστούν

Η ανάλυση ανύψωσης βρίσκει ό,τι ξεχωρίζει στα επιτυχημένα posts — και αυτό
περιλαμβάνει πράγματα που είναι **άχρηστα ή επιζήμια** για εσένα. Μετρήθηκε σε
πραγματική εκτέλεση: προτάθηκαν `#chryssanthemis` (ανύψωση 0,92×) και
`#viraltiktok`.

Τρία φίλτρα, πριν από κάθε σκοράρισμα:

| Κατηγορία | Παράδειγμα | Γιατί |
|---|---|---|
| Προσωπικό brand άλλου | `#chryssanthemis` | Δεν φέρνει ποτέ κοινό σε εσένα· δείχνει αντιγραφή |
| Άλλη πλατφόρμα | `#viraltiktok`, `#youtubeshorts` | Δεν κάνουν τίποτα σε Reel |
| Κενό σήμα | `#foryou`, `#viral`, `#l4l` | Δεκάδες εκατομμύρια posts χωρίς θεματική συνοχή |

Επιπλέον, **πάτωμα ελληνικών** 55-60% στα χαρτοφυλάκια: ένα σετ με 38%
ελληνικά hashtags δουλεύει εναντίον του σκοπού του προϊόντος. Αν δεν υπάρχουν
αρκετά ελληνικά υποψήφια, το σετ δεν κόβεται — συμπληρώνεται και το σκορ το
καταγράφει.

### 11. Ο web server δεν φέρνει εξαρτήσεις

Μια ανάλυση διαρκεί 3–5 λεπτά: ένα σύγχρονο HTTP αίτημα θα χτυπούσε timeout.
Το `/api/analyze` επιστρέφει αμέσως `job_id`, μια εργασία τρέχει σε thread, και
η διεπαφή ρωτά για πρόοδο ανά 1,5 δευτερόλεπτο. Το `Pipeline` δέχεται callback
προόδου — χωρίς αυτό, μια διεπαφή που περιμένει πέντε λεπτά είναι
δυσδιάκριτη από μια κολλημένη.

Ο server είναι `ThreadingHTTPServer` της stdlib. Σε μηχάνημα χωρίς
brew/docker/node, κάθε επιπλέον πακέτο είναι ένα ακόμη σημείο αποτυχίας για
εφαρμογή που τρέχει τοπικά για έναν χρήστη. Ακόμη και ο parser multipart είναι
δικός μας — σαράντα γραμμές αντί για το `cgi`, που αφαιρέθηκε στην Python 3.13.

Δεσμεύεται **μόνο** στο `127.0.0.1`: ο server βλέπει τα κλειδιά και τη βάση.

Μία ανάλυση τη φορά, με κλείδωμα. Δύο ταυτόχρονες θα μοιράζονταν το budget του
HikerAPI χωρίς συντονισμό και θα πλήρωναν διπλά credits για επικαλυπτόμενη
έρευνα — η ουρά είναι φθηνότερη από την παραλληλία εδώ.

---

## Το σκορ

```
Viral Score = Σ(πυλώνας × βάρος) × evidence_multiplier + synergy
```

| Πυλώνας | Βάρος | Γιατί |
|---|---|---|
| Δύναμη λεζάντας | 20% | Το μόνο που ελέγχεις πλήρως μετά το ανέβασμα |
| Ταίριασμα βίντεο–γωνίας | 18% | Θεμέλιο — λάθος γωνία, τίποτα δεν σώζει |
| Ελληνικό κοινό | 16% | Ο λόγος ύπαρξης του συστήματος |
| Ιστορικά τεκμήρια | 14% | Τι δούλεψε διαχρονικά |
| **Hashtags** | **13%** | **Σκόπιμα χαμηλότερο** — η βαρύτητά τους έχει μειωθεί |
| Τεκμήρια ανταγωνισμού | 11% | Τι δουλεύει τώρα |
| Σήμα τάσης | 8% | Μπόνους όταν υπάρχει· η απουσία δεν καταδικάζει |

**Evidence multiplier** (0,72–1,0): λογαριθμικός κορεσμός στα ελληνικά posts
και τα outliers που στήριξαν την απόφαση. Χωρίς δεδομένα HikerAPI το σύστημα
**δεν μπορεί** να βγάλει 90.

**Διάστημα εμπιστοσύνης** αντί για ψευδο-ακρίβεια: `76 (εύρος 66–86)`.

---

## Ανθεκτικότητα

| Αστοχία | Συμπεριφορά |
|---|---|
| HikerAPI 5xx / timeout | 3 retries με backoff → fallback server → **ληγμένο cache** |
| HikerAPI 401/403 | Άμεση διακοπή έρευνας, σαφές μήνυμα, η ανάλυση συνεχίζει |
| Εξάντληση budget | `BudgetExceeded` — η έρευνα σταματά, τα ήδη συλλεγμένα κρατιούνται |
| Λείπει `HIKER_API_KEY` | Λειτουργία μόνο με μνήμη, ρητή προειδοποίηση, χαμηλό σκορ |
| Λείπει ffprobe | Ανάγνωση μεταδεδομένων από το stderr του ffmpeg |
| Λείπει ASR | Κενή μεταγραφή + **ρητή δήλωση** — ποτέ φαντασία διαλόγου |
| Αποτυχία embeddings | Σιωπηλή υποχώρηση στον local embedder |
| Σπασμένη σύνδεση API | Ο πελάτης **ξαναχτίζεται** πριν το retry — το retry σε νεκρό socket απλώς ξαναποτυγχάνει |
| Υπερφόρτωση API (529) | 4 προσπάθειες με backoff έως 45s· το σφάλμα εξηγεί τι συνέβη και τι να κάνεις |
| Όλες οι λεζάντες κόπηκαν | Κρατιούνται οι 3 με τη χαμηλότερη ομοιότητα + προειδοποίηση |
| Μηδέν μετρημένα hashtags | Η φραγή 35% αίρεται, το σετ σημαίνεται ως αμέτρητο |

**Αρχή:** η υποβάθμιση είναι πάντα **δηλωμένη**. Μια ανάλυση που «πέτυχε»
κρύβοντας ότι δεν είχε δεδομένα είναι χειρότερη από αποτυχία.

---

## Κόστος ανά ανάλυση

| Πόρος | Μετρημένο σε πραγματική εκτέλεση |
|---|---|
| Κλήσεις HikerAPI | ~111 (όριο `HIKER_BUDGET_PER_RUN`, default 120) |
| Posts που συλλέχθηκαν | ~700, εκ των οποίων ~100 ελληνικά, ~14 outliers |
| Κλήσεις LLM | 3 (ανάλυση βίντεο, σχεδιασμός, λεζάντες) |
| Χρόνος | 3–5 λεπτά |

**Streaming είναι υποχρεωτικό, όχι προαιρετικό.** Μετρήθηκε: μη-streaming
κλήση με adaptive thinking και `max_tokens` 6–8K κόλλησε πάνω από 10 λεπτά και
χτύπησε το HTTP timeout του SDK — και μετά ξεκίνησαν retries. Με streaming η
ίδια κλήση ολοκληρώνεται σε ~37 δευτερόλεπτα.

Το cache μειώνει δραστικά τις επαναλήψεις: TTL 12 ώρες για προφίλ, 24 για
media, 3 για αναζητήσεις. Το `sys/balance` παρακολουθεί τα credits χωρίς χρέωση.

---

## Κλιμάκωση σε production

Η τρέχουσα εγκατάσταση (SQLite + local embeddings) αντέχει άνετα δεκάδες
χιλιάδες posts. Πέρα από εκεί:

| Στοιχείο | Τώρα | Production |
|---|---|---|
| Βάση | SQLite + WAL | Postgres (`VRGR_DB_BACKEND=postgres`) |
| Διανύσματα | hashing + numpy | pgvector + `VRGR_EMBEDDING_PROVIDER=voyage` |
| Ουρά | inline | Celery/RQ — ο orchestrator είναι ήδη ασύγχρονα διαχωρίσιμος |
| API | CLI | FastAPI πάνω από το `Pipeline` (η διεπαφή είναι έτοιμη) |
| Μετρικές | `api_log` + JSON logs | Prometheus από τα `ClientStats` |

Τα σημεία επέκτασης είναι ήδη αφηρημένα: `Repository`, `MemoryRetriever`,
`build_embedder`, `HikerClient` — καθένα πίσω από σταθερή διεπαφή.

---

## Μεθοδολογία αξιολόγησης

Το σύστημα αξιολογεί **τον εαυτό του** με πραγματικά αποτελέσματα:

1. Κάθε ανάλυση αποθηκεύει την πρόβλεψή της (`runs.predicted_score`).
2. Το `feedback` καταγράφει την πραγματική απόδοση (`outcomes`).
3. Μέτρο: **Spearman** — συσχέτιση **κατάταξης**, όχι τιμής. Το σκορ υπόσχεται
   σειρά («αυτό είναι καλύτερο από εκείνο»), όχι πρόβλεψη προβολών· το Pearson
   θα το τιμωρούσε για κάτι που ποτέ δεν υποσχέθηκε.
4. Το `calibrate` προτείνει βάρη — **μόνο** από 12 αποτελέσματα και πάνω, με
   περιορισμό μεταβολής ±25%, και **ποτέ αυτόματα**.

Το τελευταίο είναι σκόπιμο: με λίγα δείγματα, η «βελτιστοποίηση» βαρών είναι
υπερπροσαρμογή σε θόρυβο — χειρότερη από το να μην κάνεις τίποτα. Και τα βάρη
είναι η κρίση του συστήματος· δεν πρέπει να αλλάζουν σιωπηλά.
